#!/usr/bin/env python3
"""
zone‑import – Interactive / non‑interactive tool that pulls a DNS zone
              and produces Cloudflare‑compatible JSON (or pushes it directly).

(See install script for full commentary and features.)
"""

# ...existing code from the install script...
